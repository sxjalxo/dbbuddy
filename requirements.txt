# Convenience install — pyproject.toml is the source of truth.
# Use: pip install -e ".[dev]" for full setup including dev/test deps.
mysql-connector-python==8.3.0
hypothesis==6.100.1
requests==2.31.0
openai>=1.0.0
fastapi>=0.100.0
uvicorn>=0.24.0
pydantic>=2.0.0
